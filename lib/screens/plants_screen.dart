import 'package:flutter/material.dart';

class PlantsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Plants Screen', style: TextStyle(fontSize: 24)),
    );
  }
}